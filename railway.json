{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "sh /app/_build/prod/rel/showish/bin/server",
    "preDeployCommand": "sh /app/_build/prod/rel/showish/bin/migrate",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  },
  "environments": {
    "pr": {
      "deploy": {
        "startCommand": "sh -c ': \"${RAILWAY_PUBLIC_DOMAIN:?RAILWAY_PUBLIC_DOMAIN is required for PR deployments}\"; export PHX_HOST=\"$RAILWAY_PUBLIC_DOMAIN\" PREVIEW_AUTH_BROKER_URL=\"https://dev.show.laggi.sh\"; exec sh /app/_build/prod/rel/showish/bin/server'",
        "preDeployCommand": "sh -c ': \"${RAILWAY_PUBLIC_DOMAIN:?RAILWAY_PUBLIC_DOMAIN is required for PR deployments}\"; export PHX_HOST=\"$RAILWAY_PUBLIC_DOMAIN\" PREVIEW_AUTH_BROKER_URL=\"https://dev.show.laggi.sh\"; exec sh /app/_build/prod/rel/showish/bin/migrate'"
      }
    }
  }
}
